export default function({ store, redirect }) {
  // if (store.getters['modules/user/loggedUser']) {
  //   if(store.getters['modules/user/isPublisherInfuencer'] || !store.getters['modules/user/isPublisher']){
  //     redirect('/dashboard');
  //   } else {
  //     redirect('/publisher-report');
  //   }
  // } else {
  //   redirect('/login');
  // }
}